const env = process.env

const config = {
    db: { 
      user:'root',
      host: 'localhost',
      password: 'root',
      database: 'movieDB',
      port: 3306
    },
  };
  
  
  module.exports = config;